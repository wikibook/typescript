function toSeconds(hours: number, minutes: number, seconds: number): number {
  // 세미콜론을 생략하지 않은 return 문
  return hours * 3600 + minutes * 60 + seconds;
  // 세미콜론을 생략한 return 문
  return hours * 3600 + minutes * 60 + seconds
}
